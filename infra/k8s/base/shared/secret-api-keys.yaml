# Template — DO NOT commit real secrets here.
# Create the actual secret from your .env file:
#   kubectl create secret generic api-keys --from-env-file=.env -n fable
#
# Or manually:
#   kubectl create secret generic api-keys \
#     --from-literal=OPENROUTER_API_KEY=sk-... \
#     --from-literal=OPENAI_API_KEY=sk-... \
#     -n fable
apiVersion: v1
kind: Secret
metadata:
  name: api-keys
  namespace: fable
type: Opaque
data: {}
