apiVersion: v1
kind: Service
metadata:
  name: coordinator
  namespace: fable
spec:
  type: NodePort
  selector:
    app: coordinator
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30000  # Maps to localhost:8000 via kind extraPortMappings
